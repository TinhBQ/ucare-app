//unify BlocProvider and routes and pages
import 'package:flutter/material.dart';

class PageEntity {
  String route;
  Widget page;

  PageEntity({
    required this.route,
    required this.page,
  });
}
